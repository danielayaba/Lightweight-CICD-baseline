{
  "name": "lightweight-cicd-poc",
  "version": "1.0.0",
  "description": "Proof-of-concept lightweight CI/CD pipeline baseline application",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "eslint .",
    "build": "echo \"No build step for plain JS — placeholder for TS projects\" && exit 0"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
